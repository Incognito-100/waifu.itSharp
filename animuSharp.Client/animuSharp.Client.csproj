<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net7.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <GenerateDocumentationFile>True</GenerateDocumentationFile>
    <GeneratePackageOnBuild>True</GeneratePackageOnBuild>
    <PackageId>animuSharp</PackageId>
    <Title> animuSharpThe waifu.it C# wrapper</Title>
    <Version>2.0.0</Version>
    <Authors>IVH</Authors>
    <Company>IVH</Company>
    <Product>animuSharp</Product>
    <Description>a C# wrapper for waifu.it</Description>
    <PackageProjectUrl>https://github.com/Incognito-100/waifu.itSharp</PackageProjectUrl>
    <RepositoryUrl>https://github.com/Incognito-100/waifu.itSharp</RepositoryUrl>
    <PackageTags>waifu.it,c#,wrapper</PackageTags>
    <PackageReleaseNotes>changed url to new  waifu.it (its just animu with a different name)
removed waifu and data.cd and moved them into one file
hid the GetResponse method from client class</PackageReleaseNotes>
    <AssemblyVersion>1.2.0.0</AssemblyVersion>
    <FileVersion>1.2.0.0</FileVersion>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Debug|AnyCPU'">
    <DebugType>embedded</DebugType>
  </PropertyGroup>

  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <DebugType>embedded</DebugType>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
  </ItemGroup>

</Project>
